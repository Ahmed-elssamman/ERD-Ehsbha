# Test Inventory

## API spec files

Pending — enumerate `apps/api/src/**/*.spec.ts` files.

## API scripts

- `apps/api/scripts/` — enumeration pending

## Test commands from manifests

Pending — enumerate from workspace package.json files.

## Missing tests

- Web tests — missing
- Admin tests — missing
- Integration tests — missing
- Contract tests — missing
- E2E tests — absent (not_applicable)
