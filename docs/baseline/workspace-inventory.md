# Workspace Inventory

## Workspaces from package.json

- `apps/*` — matches `apps/api`, `apps/web`, `apps/admin`
- `packages/*` — matches package directories

## Matching Package Directories

- `apps/api` — @ehsbha/api
- `apps/web` — @ehsbha/web
- `apps/admin` — @ehsbha/admin

## Stale Root Package Entries

- `backend` — legacy entry (to be removed from lockfile)
- `web` — legacy entry (to be removed from lockfile)
